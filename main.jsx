import React from 'react';
import ReactDOM from 'react-dom';
import App from './app/components/App';

// App.propTypes = { children: React.PropTypes.string };
// export default App;
ReactDOM.render( <App title="Select A Life Goal" />, document.getElementById('app'));
// const HelloMessage = (props) => <h2>Hello {props.name}</h2>;
