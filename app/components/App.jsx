import React from 'react';

const App = (props) => (
  <div>
    <h2>{props.title}</h2>
    <select>
      <option>Love</option>
      <option>Wealth</option>
      <option>Health</option>
    </select>
  </div>
);

export default App;
